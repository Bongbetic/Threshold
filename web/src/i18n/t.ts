/**
 * i18n stub - English-only v1.
 *
 * Convention: semantic keys, English source strings.
 * Future: xgettext reads calls directly.
 */

/**
 * Return the English string for the given key.
 * In v1, the key IS the English string.
 */
export function t(key: string): string {
  return key;
}
