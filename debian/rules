#!/usr/bin/make -f

export DH_VERBOSE = 1

%:
	dh $@ --buildsystem=meson --with python3

override_dh_auto_install:
	cd obj-x86_64-linux-gnu && DESTDIR=$(CURDIR)/debian/tmp ninja install

override_dh_python3:
	dh_python3 usr/share/com.bongbetic.threshold
