Source: threshold
Section: utils
Priority: optional
Maintainer: Soubarna <Soubarna@live.in>
Build-Depends: debhelper-compat (= 13),
               dh-python,
               meson (>= 1.0),
               ninja-build,
               pkg-config,
               desktop-file-utils,
               gettext (>= 0.19.8),
               gobject-introspection,
               python3,
               python3-gi,
               python3-pytest,
               libgtk-4-dev,
               libadwaita-1-dev,
               gir1.2-gtk-4.0,
               gir1.2-adw-1,
               gir1.2-notify-0.7
Standards-Version: 4.7.0
Homepage: https://github.com/Bongbetic/Threshold
Vcs-Browser: https://github.com/Bongbetic/Threshold
Vcs-Git: https://github.com/Bongbetic/Threshold.git
Rules-Requires-Root: no

Package: threshold
Architecture: amd64
Depends: ${python3:Depends},
         ${misc:Depends},
         dkms (>= 2.2),
         gir1.2-dbusmenu-glib-0.4,
         python3-gi,
         gir1.2-gtk-4.0,
         gir1.2-adw-1,
         gir1.2-notify-0.7
Recommends:
Description: Battery charge threshold controller for Linux laptops
 Threshold provides a GTK4 graphical interface for setting the
 battery charge threshold on Linux laptops. When the msi-ec kernel
 module is available the charge limit is written directly to the EC
 microcontroller and persists across reboots. When no EC/sysfs charge
 control is available the application falls back to a notification
 alarm that alerts the user once the charge threshold is reached.
 .
 Includes a bundled DKMS source for the msi-ec kernel module which
 is built automatically against the running kernel at install time.
