#!/bin/sh
set -e

case "$1" in
    remove|upgrade|deconfigure)
        if command -v dkms >/dev/null 2>&1; then
            dkms remove -m msi-ec -v 0.13 --all --no-depmod || true
        fi
        ;;
esac

exit 0
